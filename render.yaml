services:
  # Backend API Service
  - type: web
    name: cybermauro-blog
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://cybermauro-blog-1.onrender.com

  # Frontend Static Site
  - type: static
    name: cybermauro-blog-1
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    envVars:
      - key: REACT_APP_API_URL
        value: https://cybermauro-blog.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
